wrapper: TorchvisionDatasetWrapper #name of class within torchvision.py
module: torchvision
name: ???
type: torchvision
#sizes are arbitrary as the data is already pre-split
sizes:
defaults:
  - dataloader/default