command: export
# model_chkpt_dir: ${oc.env:HOME}
model_dir: models #name of directory in qtransform/outputs
from_checkpoint: ???

export_fn: qonnx  # brevitas export ot troch onnx.export #  can be export, export_brevitas_onnx, export_qonnx, export_onnx_qcdq
opset_version: 16

do_constant_folding: True ### found in the onnx docs, not sure what this does, param for opnnx.export func, currently unsused

#root_path: ~/.qtransform/onnx/ #absolute path to directory where onnx models should be saved. default: base directory of qtransform